ASE's Lennard-Jones pair potential, in-process. The defaults are
dimensionless (epsilon = sigma = 1) and describe no real material; pass
`epsilon=`, `sigma=`, and `rc=` in `calculator_options` if you use it at
all. Fit for testing workflow mechanics, not for science.
